
pub struct Picture {

}

/// takes a picture non-blockingly, and returns a String with an error message if it failed for
/// some reason
pub fn take_picture() -> Result<Picture, String> {
    Err(String::from("take_picture not unimplemented!"))
}
